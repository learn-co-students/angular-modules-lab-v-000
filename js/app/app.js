angular
      .module("app", ['ngRoute']);
 // The number of parameters is true for everything we set/get in Angular. If
 // we only have one parameter, we are telling Angular to get.
 // If we have two parameters, we are telling Angular to set.
